apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubestudio.fullname" . }}
  labels:
    {{- include "kubestudio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubestudio.selectorLabels" . | nindent 4 }}
